选择代码 if  else  
int main()
{
	int g = 0;
	printf("是否每天努力敲代码?(1/0)\n");
	scanf("%d", &g);//输入的东西后面都没有换行，暂时还不知道为什么？
	//加了\n换行就会要多一次数字才行。
	if (g == 1)
		printf("好好敲代码你一定会得到一个好工作的。");
	else
		printf("自生自灭吧！");

	return 0;
}
